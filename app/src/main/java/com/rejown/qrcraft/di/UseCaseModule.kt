package com.rejown.qrcraft.di

import org.koin.dsl.module

val useCaseModule = module {
    // Use cases will be added as we implement features
    // Example:
    // factory { ProcessScanResultUseCase(get()) }
    // factory { SaveScanHistoryUseCase(get()) }
}
